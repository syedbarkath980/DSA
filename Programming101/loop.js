/* While loop repeats the block as long as the condition is true. */

let count = 1;
while (count <= 5) {
  console.log("Count is: " + count);
  count++;
}

/* For loop is often used when the number of iterations is known. */

for (let i = 1; i <= 5; i++) {
  console.log("Iteration: " + i);
}
